---
import LocalizedText from '../components/LocalizedText.astro';
import { getCollection } from 'astro:content';
import Site from '../layouts/Site.astro';
import PersonCard from '../components/people/PersonCard.astro';
import PersonLead from '../components/people/PersonLead.astro';

import '../styles/people.css';

const all = await getCollection('people');
const byOrder = (a: typeof all[number], b: typeof all[number]) =>
  a.data.order - b.data.order;

const lead = all.find((p) => p.data.group === 'faculty');

const STAFF_RANK: Record<string, number> = { postdoc: 0, phd: 1, faculty: 2 };
const staff = all
  .filter((p) => p.data.group in STAFF_RANK && p.id !== lead?.id)
  .sort((a, b) => {
    const r = (STAFF_RANK[a.data.group] ?? 9) - (STAFF_RANK[b.data.group] ?? 9);
    return r !== 0 ? r : byOrder(a, b);
  });

const partners = all.filter((p) => p.data.group === 'partner').sort(byOrder);

const partnersByOrg = [
  ...partners
    .reduce((acc, p) => {
      const org = p.data.org ?? 'Other';
      (acc.get(org) ?? acc.set(org, []).get(org)!).push(p);
      return acc;
    }, new Map<string, typeof partners>())
    .entries(),
].sort((a, b) => a[1][0].data.order - b[1][0].data.order);

const alumni = all.filter((p) => p.data.group === 'alumni').sort(byOrder);
---

<Site titleRu="О группе · ООИПИГ" descriptionRu="Исследовательская Группа Онлайн Оптимизация И Приложения: руководитель, команда и выпускники."
  active="about"
  title="About · OOAARG"
  description="Online Optimization And Applications Research Group: lead, staff, and alumni."
>
  <section class="people-hero">
    <div class="container">
      <h1 class="about-title" data-i18n="Online Optimization And Applications Research Group">Online Optimization And Applications Research Group</h1>
      <p style="margin-top: 24px; color: var(--fg-muted); max-width: 80ch; font-size: 18px;" data-i18n="A research group studying decisions under uncertainty. Theory, algorithms, and the messy real-world problems that motivate both.">
          A research group studying decisions under uncertainty. Theory, algorithms, and the messy real-world problems that motivate both.
      </p>
    </div>
  </section>

  {lead && (
    <section class="people-section">
      <div class="container">
        <h2 class="kicker" data-i18n="Lead">Lead</h2>
        <PersonLead person={lead} />
      </div>
    </section>
  )}

  {staff.length > 0 && (
    <section class="people-section">
      <div class="container">
        <h2 class="kicker" data-i18n="Staff">Staff</h2>
        <div class="people-grid">
          {staff.map((p) => (
            <a class="person-card-link" href={`/about/${p.id}`}>
              <PersonCard person={p} />
            </a>
          ))}
        </div>
      </div>
    </section>
  )}

  {partners.length > 0 && (
    <section class="people-section">
      <div class="container">
        <h2 class="kicker" data-i18n="Partners">Partners</h2>
        <div class="partner-orgs">
          {partnersByOrg.map(([org, members]) => (
            <div class="partner-org">
              <div class="partner-org-label">
                <span class="partner-org-marker" aria-hidden="true"></span>
                <span class="partner-org-name"><LocalizedText en={org} ru={members.find((p) => p.data.ru?.org)?.data.ru?.org} /></span>
              </div>
              <div class="people-grid">
                {members.map((p) => (
                  <a class="person-card-link" href={`/about/${p.id}`}>
                    <PersonCard person={p} />
                  </a>
                ))}
              </div>
            </div>
          ))}
        </div>
      </div>
    </section>
  )}

  {alumni.length > 0 && (
    <section class="people-section">
      <div class="container">
        <h2 class="kicker" data-i18n="Alumni">Alumni</h2>
        <div class="people-grid">
          {alumni.map((p) => (
            <a class="person-card-link" href={`/about/${p.id}`}>
              <PersonCard person={p} />
            </a>
          ))}
        </div>
      </div>
    </section>
  )}
</Site>
